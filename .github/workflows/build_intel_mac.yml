name: Build PyTorch for Intel Mac

on:
  workflow_dispatch:
    inputs:
      pytorch_version:
        description: 'PyTorch version tag (e.g. v2.6.0)'
        required: true
        type: string
        default: 'v2.9.1'
      macos_version:
        description: 'macOS runner version (15 or 26)'
        required: true
        type: choice
        options:
          - '15'
          - '26'
      python_version:
        description: 'Python version (e.g. 3.10)'
        required: true
        type: string
        default: '3.10'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.pytorch_version }}-${{ inputs.macos_version }}-${{ inputs.python_version }}
  cancel-in-progress: true

jobs:
  check-existing:
    runs-on: ubuntu-latest
    outputs:
      skip: ${{ steps.check.outputs.skip }}
      tag_name: ${{ steps.vars.outputs.tag_name }}
      wheel_name: ${{ steps.vars.outputs.wheel_name }}
    steps:
      - name: Set variables
        id: vars
        run: |
          VER_NO_V="${PT_VER#v}"
          PY_ABI="cp${PY_VER//.}"
          MACOS_DEPLOY_TARGET="10.15"
          WHEEL_NAME="torch-${VER_NO_V}-${PY_ABI}-${PY_ABI}-macosx_${MACOS_DEPLOY_TARGET}_x86_64.whl"
          TAG_NAME="torch-${VER_NO_V}-macos${MACOS_VER}-py${PY_VER}"
          echo "tag_name=${TAG_NAME}" >> $GITHUB_OUTPUT
          echo "wheel_name=${WHEEL_NAME}" >> $GITHUB_OUTPUT
        env:
          PT_VER: ${{ inputs.pytorch_version }}
          PY_VER: ${{ inputs.python_version }}
          MACOS_VER: ${{ inputs.macos_version }}

      - name: Check if release asset already exists
        id: check
        run: |
          if ASSETS=$(gh release view "${{ steps.vars.outputs.tag_name }}" --json assets -q ".assets[].name" 2>/dev/null); then
            if echo "$ASSETS" | grep -qF "${{ steps.vars.outputs.wheel_name }}"; then
              echo "skip=true" >> $GITHUB_OUTPUT
              echo "Release with the same wheel already exists. Skipping build."
            else
              echo "skip=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "No existing release found for tag ${{ steps.vars.outputs.tag_name }}."
          fi
        env:
          GH_TOKEN: ${{ github.token }}

  build:
    needs: check-existing
    if: needs.check-existing.outputs.skip == 'false'
    runs-on: ${{ inputs.macos_version == '15' && 'macos-15-intel' || 'macos-26-intel' }}
    timeout-minutes: 720
    steps:
      - name: Checkout PyTorch source code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.pytorch_version }}
          submodules: recursive

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          python-version: ${{ inputs.python_version }}
          miniforge-version: latest
          activate-environment: build

      # 移除 intel-openmp，只保留 MKL 本身
      - name: Install build & runtime dependencies
        shell: bash -el {0}
        run: |
          conda install -y \
            mkl mkl-include \
            numpy ninja pyyaml setuptools cmake cffi \
            typing_extensions future six requests dataclasses
          brew install cmake ninja ccache
          ccache -M 5G

      - name: Cache ccache objects
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/ccache
          key: ${{ runner.os }}-ccache-${{ inputs.pytorch_version }}-${{ inputs.python_version }}-v4
          restore-keys: |
            ${{ runner.os }}-ccache-${{ inputs.pytorch_version }}-${{ inputs.python_version }}-

      - name: Restore ccache environment
        run: |
          mkdir -p ${{ github.workspace }}/ccache
          echo "CCACHE_DIR=${{ github.workspace }}/ccache" >> $GITHUB_ENV
          echo "/usr/local/opt/ccache/libexec" >> $GITHUB_PATH

      - name: Fix script permissions
        run: |
          chmod +x scripts/build_host_protoc.sh
          find scripts -name "*.sh" -exec chmod +x {} \;

      - name: Build PyTorch wheel (CPU+MKL, no tests)
        shell: bash -el {0}
        run: |
          export MACOSX_DEPLOYMENT_TARGET=10.15
          export CMAKE_OSX_ARCHITECTURES=x86_64
          export CMAKE_POLICY_VERSION_MINIMUM=3.5

          export USE_MKL=1
          export BLAS=MKL
          export BUILD_TEST=0
          export USE_MPS=0
          export USE_METAL=0
          export USE_VULKAN=0
          export USE_NCCL=0
          export USE_DISTRIBUTED=0
          export USE_CUDA=0
          export BUILD_DIR=$PWD/build  # 传递构建目录

          python setup.py bdist_wheel

      # 核心修复步骤：在 delocate 之前修复 OMP 路径
      - name: Fix OpenMP paths for delocate
        shell: bash -el {0}
        run: |
          python -c "
          import os, shutil, subprocess
          from pathlib import Path

          # 此时 wheel 刚刚生成完成，构建产物在 build/lib 中
          build_base = os.environ.get('BUILD_DIR', 'build')
          lib_dir = Path(build_base) / 'lib' / 'torch' / 'lib'
          libtorch_cpu = lib_dir / 'libtorch_cpu.dylib'

          if not libtorch_cpu.exists():
              print(f'Error: {libtorch_cpu} not found')
              exit(1)

          # 查找 OMP 库
          omp_lib_name = 'libiomp5.dylib'
          source_omp = Path(os.environ['CONDA_PREFIX']) / 'lib' / omp_lib_name
          if not source_omp.exists():
              # Fallback to brew
              brew_prefix = subprocess.run(['brew', '--prefix'], capture_output=True, text=True).stdout.strip()
              source_omp = Path(brew_prefix) / 'lib' / omp_lib_name
          if not source_omp.exists():
              print(f'Error: Could not find {omp_lib_name}')
              exit(1)

          # 复制并修改
          target_omp = lib_dir / omp_lib_name
          if not target_omp.exists():
              shutil.copy2(source_omp, target_omp)
              print(f'Copied {source_omp} -> {target_omp}')

          omp_rpath_path = '@rpath/libiomp5.dylib'
          omp_loader_path = '@loader_path/libiomp5.dylib'
          
          # 清理旧 rpath
          output = subprocess.run(['otool', '-l', str(libtorch_cpu)], capture_output=True, text=True).stdout
          for line in output.splitlines():
              if line.strip().startswith('path '):
                  old_rpath = line.strip().split(' ', 1)[1]
                  subprocess.run(['install_name_tool', '-delete_rpath', old_rpath, str(libtorch_cpu)], check=False)
          
          # 更新库引用
          subprocess.run(['install_name_tool', '-change', omp_rpath_path, omp_loader_path, str(libtorch_cpu)], check=True)
          print('OpenMP path fix applied.')
          "

      - name: Bundle shared libraries with delocate
        shell: bash -el {0}
        run: |
          python -m pip install delocate
          export DYLD_LIBRARY_PATH=$CONDA_PREFIX/lib:$DYLD_LIBRARY_PATH
          delocate-wheel -v dist/*.whl

      - name: Locate wheel file
        id: wheel
        shell: bash
        run: |
          WHL=$(ls dist/*.whl | head -1)
          echo "path=${WHL}" >> $GITHUB_OUTPUT
          echo "name=$(basename ${WHL})" >> $GITHUB_OUTPUT

      - name: Upload wheel as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: pytorch-intel-wheel
          path: ${{ steps.wheel.outputs.path }}

      - name: Create/Update Release and upload wheel
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.check-existing.outputs.tag_name }}
          name: "PyTorch ${{ inputs.pytorch_version }} • macOS ${{ inputs.macos_version }} • Python ${{ inputs.python_version }}"
          body: |
            **Community Build for Intel Mac** (OpenMP conflict fixed)
            - PyTorch version: `${{ inputs.pytorch_version }}`
            - Built on: `macos-${{ inputs.macos_version }}-intel`
            - Python: `${{ inputs.python_version }}`
            - Wheel: `${{ steps.wheel.outputs.name }}`

            ** Contains a fix for OMP: Error #15 **
            ```
            pip install ${{ steps.wheel.outputs.name }}
            ```
          files: ${{ steps.wheel.outputs.path }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ github.token }}
